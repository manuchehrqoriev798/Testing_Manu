// Common utility functions for data structure visualization

export const generateRandomId = () => {
  return Math.random().toString(36).substring(2, 15);
};

export const calculateNodePositions = (nodes, width, height) => {
  // Calculate positions for tree/graph nodes
  // This is a placeholder - implement specific logic based on data structure needs
};

export const animateTransition = (element, duration = 300) => {
  // Helper for animations
};
